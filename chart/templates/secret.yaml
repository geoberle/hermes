apiVersion: v1
kind: Secret
metadata:
  name: hermes-agent-secrets
  namespace: hermes
  labels:
    {{- include "hermes.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
